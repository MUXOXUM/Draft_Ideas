Минималистичная веб-страница с подборкой оптических иллюзий.

При открытии пользователь сразу видит иллюзию в центре экрана без подписей. 
Переключение между изображениями выполняется только с клавиатуры:

- `←` и `→`
- `A` и `D`
